version: "2"
linters:
  default: all
  disable:
    - depguard
    - exhaustruct
    - nonamedreturns
    - paralleltest
    - varnamelen
    - wrapcheck
    - wsl
  settings:
    gosec:
      excludes:
        - G104
    nolintlint:
      require-explanation: true
      require-specific: true
      allow-unused: false
  exclusions:
    rules:
      - path: _test\.go
        linters:
          - cyclop
          - funlen
          - gocognit
          - lll
          - prealloc
formatters:
  enable:
    - gci
    - gofmt
    - gofumpt
    - goimports
